# Generar el mapa en SVG

Toma el JSON anterior y conviértelo a SVG como una imagen 100x100 (usa los valores directamente, sin porcentages) y asigna el ID exacto a cada elemento (ej: id='chimenea').
No le agregues estilos.
