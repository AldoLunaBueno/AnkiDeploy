# Generar coordenadas del mapa

Dame un objeto JSON donde las claves son los nombres de estos objetos: LISTA_DE_OBJETOS.
Y los valores de las claves son arrays [ymin, xmin, ymax, xmax] normalizados de 0 a 100 (porcentajes de la imagen).
Sé lo más preciso posible con los bordes.
